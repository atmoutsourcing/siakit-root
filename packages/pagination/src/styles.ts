import { styled } from '@siakit/core'

const Container = styled('div', {
  display: 'flex',
  flexWrap: 'wrap',

  justifyContent: 'space-between',
  alignItems: 'center',

  width: '100%',
})

export { Container }
